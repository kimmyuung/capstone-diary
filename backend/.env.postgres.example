# 개발 환경 PostgreSQL 설정
# Docker에서 PostgreSQL을 실행할 때 사용

# PostgreSQL 설정 (개발용)
POSTGRES_DB=diary_db
POSTGRES_USER=diary_user
POSTGRES_PASSWORD=diary_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# 기존 .env 파일의 다른 설정들도 그대로 유지하세요
# GEMINI_API_KEY=...
# SECRET_KEY=...
